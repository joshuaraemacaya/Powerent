extends ../template/master.pug

block content
    section.hero-item
            .boxcontain
                h1.title 
                    font(size="7" color="white") All Items
                    br
                    <centeR>
                    font(size="5" color="white")= `Welcome! ${online}` 
                    a.button.is-info.is-pulled-right(href="/org") View Organizations 
                    hr
                    .column
                    .columns.is-multiline
                        each item in items
                                .column.is-3
                                    a(href=`/item?id=${item.item_id}`)
                                        .box
                                            .field(style="color: black") 
                                                <center>
                                                    strong= item.item_name   
                                                .field
                                                    hr
                                                    h1name= item.item_desc
                                                    